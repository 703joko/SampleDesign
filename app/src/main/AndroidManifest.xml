<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.preferencesettings">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PreferenceSettings">
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- NAD Team dialog -->
        <activity android:name="com.nusantara.wings.fragments.team.TeamActivity"
            android:taskAffinity=""
            android:parentActivityName=".SettingsActivity"
            android:exported="true"
            android:theme="@style/TeamDialog"
            android:label="@string/dialog_team_title" />

    </application>

</manifest>